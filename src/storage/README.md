# SQLite storage

The schema and database-opening code define how documents, passages, runs, and measurements are stored. SQLite keeps this small application’s persistent state in one place.

This folder contains storage code and tests, not the mutable database itself. See the [root README](../../README.md) for runtime data configuration.
