# Evaluation runner

This module runs the question set through the supported answer modes and records results for comparison. It keeps the test procedure separate from the researcher itself.

Inputs live in [eval](../../../eval/README.md), saved runs in [artifacts/evaluation](../../../artifacts/evaluation/README.md), and the reported outcomes in [EVAL.md](../../../EVAL.md).
