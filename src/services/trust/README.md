# Evidence-quality signals

This module computes an explainable score from evidence signals to help readers understand an answer’s support. It is a heuristic, not a calibrated probability that the answer is true.

The score does not override failed answer checks. See [answer validation](../answer/README.md) for the decisions that control what can be returned.
