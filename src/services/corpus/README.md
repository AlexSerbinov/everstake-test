# Browse the collection

This module exposes the stored collection for browsing, filtering, and inspection. It helps a reviewer see which documents are available and inspect their metadata.

It reads the corpus; it does not crawl sources or generate answers. Those jobs belong to [crawler](../crawler/README.md) and [answer](../answer/README.md).
