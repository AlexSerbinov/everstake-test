# Researcher evidence guidance

This folder contains reusable guidance that the application loads alongside the researcher’s prompt. It explains how to compare sources, treat dates and copied claims, and handle insufficient evidence.

These are instructions for the application’s researcher, not development-agent plugins. The [agent configuration](../agents/README.md) selects them; code still performs its own checks.
