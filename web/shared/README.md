# Shared browser helpers

Small helpers handle common DOM construction, safe links, formatting, and source-date labels. Features reuse them so the same information is presented consistently.

Feature-specific behavior belongs in [features](../features/README.md); this folder holds only shared browser utilities.
