# Knowledge-base summary

The summary panel brings together collection size, a saved evaluation, known API costs, and source-update status. It also explains the collection’s limits.

Each figure retains its own scope: current corpus data, a particular evaluation run, and all-time known costs are not measurements of the same event.
