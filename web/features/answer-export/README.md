# Export an answer

This module turns the delivered answer into readable text with its dated claims, status, corpus version, and source links. Exports use the same answer data as the interface.

It formats an existing result; it does not ask the model for a new answer.
