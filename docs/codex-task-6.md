You are continuing YOUR OWN work in `codex-work/` (same rules: read SUMMARY/REPORT/README and the code first; deployed at https://everstake-codex.89-167-19-222.sslip.io; keys in ./.env; Gemini 3.x only; do not read ../claude-work/; commit only codex-work/ paths; do NOT git push; API spend ≤ ~$1).

Implement `../docs/trust-score-task.md` for your version, in your own way: a deterministic Trust Score (0–100) on every answer with a component breakdown (source authority, independent agreement with dedup/syndication counted once, recency, grounding gates, extraction confidence, model self-assessment as a minor separate component), plain-language reasons per component, bands with labels, badge + breakdown in the UI, `trust` in the API and stream, live-editable weights, unit tests including the two worked examples, a Trust Score column in your eval/adversarial reports with the correlation to correctness, REPORT/README/SUMMARY updates, deploy and verify.
