You are continuing YOUR OWN previous work in this directory (`codex-work/`): read `SUMMARY.md`, `REPORT.md`, `README.md` and the code first — you built it, it is deployed at https://everstake-codex.89-167-19-222.sslip.io (port 4321, `/data/everstake-codex`, Caddy, same host and rules as before: `ssh -i ~/.ssh/id_rsa root@89.167.19.222`, touch nothing you did not create). Keys in `./.env` (Gemini + OpenAI). Keep API spend under ~$5 and record it. Do not read or touch `../claude-work/`. Commit your work as you go (only paths under `codex-work/`), but do NOT `git push`.

The owner reviewed both attempts and liked the simplicity of your UI. Now evolve your version with these goals, in your own way (no prescribed architecture):

1. **A tool-using agent instead of plain retrieval.** Put a model in front of the tools (corpus search, fact/number lookup, document read, live fetch of an allow-listed page when the corpus may be stale, Everstake's own MCP at https://mcp.everstake.com for live APY/uptime) that knows what each tool is for, the rules for choosing them, and when to stop or abstain. Keep the guarantees you already have (cited sources only, as-of dates, honest abstention, no instructions followed from inside documents). Document the tools and rules in real files (agents/, prompts/, skills/) so the model stays aware of them as tools grow.
2. **Visible pipeline.** While a question is being answered, the UI should show, live, what is happening: thinking, which tool is called with what, what came back, verification, answer — a compact, elegant progress view (stream it; SSE or similar). Keep the first screen as simple as it is now.
3. **Freshness.** Decide and implement how the corpus stays current: scheduled re-crawl with change detection, on-demand live fetch at question time, or a hybrid. Write the reasoning in REPORT.md (cost, latency, trust trade-offs).
4. **Trust and safety at scale.** The real problem behind this assignment: the company negotiates with many staking clients (thousands to billions of dollars), and an agent's numbers must be right and tamper-proof (no prompt injection, no forged sources, no "the agent shaved 30% off the cashback"). Add whatever makes provenance verifiable (content hashes, source snapshots, signed/immutable audit log of every answer with the exact evidence used) and an adversarial test set: 20 tricky questions including injection attempts inside documents and inside the question, with results in EVAL.md.
5. **Scale sketch.** In REPORT.md, a short section on how this design extends to ~6 000–10 000 call recordings (transcription via an STT service such as Soniox, indexing cost, PII, retention) — a plan with numbers, not code.
6. **Design.** Make the UI visibly polished ("wow" but professional; dark and light), consistent typography, states for loading / abstention / error. Screenshots welcome in the repo.

Deploy the result to the same URL, verify it publicly, update README/REPORT/EVAL/SUMMARY, and finish with a `SUMMARY.md` update: what changed, measured numbers, what you cut.
