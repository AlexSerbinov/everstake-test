You are continuing YOUR OWN work in `codex-work/` (read SUMMARY.md, REPORT.md, README.md and the code first; deployed at https://everstake-codex.89-167-19-222.sslip.io, port 4321, `/data/everstake-codex`, Caddy; `ssh -i ~/.ssh/id_rsa root@89.167.19.222`; touch nothing you did not create; keys in `./.env`; Gemini 3.x only; do not read `../claude-work/`; commit only paths under `codex-work/`; do NOT `git push`).

Implement `../docs/freshness-task.md` for your version, in your own way: a per-source-type freshness policy (interval + depth, three presets + custom), a real refresh job with cheap change detection (sitemap lastmod → conditional GET → content hash; GitHub via the org repos list `pushed_at` and commits-since), a change log shown in the UI ("last refreshed / what changed"), and a **live calculator in the UI** driven by your measured unit costs: sliders/dropdowns per source type → monthly $, tokens, machine minutes, worst-case staleness, with a small chart and plain-language sentences. Run one real refresh, keep API spend under ~$0.50, add tests for the calculator math, update REPORT/README/SUMMARY, deploy and verify the public URL.
