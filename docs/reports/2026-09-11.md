# Звіт за 11.09.2026

**Стан:** система працює наскрізь і задеплоєна — 441 документ у корпусі, 20 питань eval прогнано, 0 вигаданих фактів, публічний URL `https://everstake.89-167-19-222.sslip.io`.
**Витрачено за день:** ≈ $2.6 (індекс $2.19 · eval і запити ≈ $0.4)

## Зроблено

- **17:04** — отримано тестове завдання від Everstake (60 seed-URL у `corpus_sources.csv`, вікно 5 днів). Завдання перекладено українською дослівно для роботи — `docs/TEST_ASSIGNMENT_UA.md`.
- **19:30–20:15 — розвідка до першого рядка коду.** Прочитано `everstake.com/ai-info` і `/mcp`; знайдено пастку з CEO і секцію «Guidelines for AI assistants». Викликано живий MCP Everstake по JSON-RPC: 11 інструментів, 27 активних мереж проти «130+» у статичному профілі, ~1.7k токенів описів інструментів на запит. Обстежено краулабельність усіх 60 URL (`docs/crawl-survey.md`): robots.txt на 28 хостах, 735 URL у sitemap, редиректи `.one → .com`, зіпсоване число `735,,,,` у мета-описі signalplus.
- **20:20–20:40** — побудова за планом `docs/PLAN.md`, покроковими комітами: scaffold → crawler → dedup → index → facts → ask → api+ui → mcp+skill+agents → eval+cost → docker+deploy.
- **20:45** — індекс зібрано: 2 138 чанків з ембеддингами, ledger фактів витягнуто Haiku 4.5 ($2.14).
- **20:50** — перший живий тест провалився: питання про CEO дало «Kinitsky». Додано правило «жива сторінка станом на дату фетчу» + гігієна ledger → відповідь виправилась на «Vasylchuk, зі зміною 2025 як історією».
- **21:00** — перший прогін eval: 9 питань, усі correct, далі впертись у денний ліміт OpenRouter. Помилки провайдера відокремлено від відмов (`gate: model_error`).
- **21:15** — деплой на Hetzner через Docker + Caddy, написано `REPORT.md`.
- **21:50** — скінчились кредити Claude на всіх ключах машини. Додано провайдера Gemini, повний eval перепрогнано: 20/20 оцінено, 0 галюцинацій, strict 65% / lenient 100%. Виправлено баг таймлайну, знайдений питанням q15 (у ledger подавались лише значення 2026 року).
- **22:20** — чеклист відповідності завданню (`docs/CHECKLIST.md`) і дизайн-бриф на UI-прохід.
- **22:30** — створено GitHub-репозиторій; запущено другу, незалежну реалізацію іншою моделлю (GPT-5.6 через Codex) у `codex-work/` — ті самі входи, жодних підказок щодо архітектури. Результат: Python, 280 документів, 1 075 чанків, свої EVAL/REPORT, окремий деплой. Сліпа перевірка: питання CEO — правильно, негативний кейс — відмова, питання про зростання мереж «пливе» на кількість IBC-чейнів Cosmos (таймлайну немає).
- **22:45** — модель відповідей переведено на `gemini-3.8-flash`. Фінальний прогін: 0 галюцинацій, 0 неправильних, 5/5 негативних відмов, strict 70% / lenient 95%, $0.011 за питання. Питання про зростання мереж тепер повертає повну траєкторію (70+ → 85+ → 130+ історично / 30+ активних).

## Рішення дня

| Рішення | Альтернатива | Чому так |
|---|---|---|
| Свій пайплайн на TypeScript, ~1 800 рядків | LangChain / LlamaIndex | На захисті треба міняти будь-який рядок наживо; фреймворк ховає саме ту логіку, яку оцінюють (ранжування, гейти, стрипінг) |
| SQLite (`node:sqlite`) з FTS5 і векторами як BLOB | Векторна БД | 2 138 чанків — косинус у процесі за десятки мс; окрема БД тут церемонія, а не архітектура |
| 250 найновіших постів блогу з 629 | Усі 629 | Вартість індексу лінійна, старі пости дають лише історію; seed-пости 2019–2022 усе одно завантажено окремо |
| Стрипінг AI-інструкцій **на індексації**, в окрему таблицю | Попросити модель ігнорувати їх у промті | Промт — це порада, таблиця — факт. Рецензент може зробити `SELECT * FROM instructions`; модель не можна відмовити від таблиці, якої вона не отримала |
| Правило «жива сторінка станом на дату фетчу» перебиває старіше датоване оголошення | «Найновіше датоване виграє» | Прес-реліз червня 2025 синдиковано 9 разів і він датований; сторінка компанії без дати описує теперішнє. Без правила відповідь про CEO неправильна |
| Друга незалежна реалізація на GPT-5.6 | Одна реалізація | Дає чесний бейзлайн «а що дала б інша модель на тих самих входах» — і показує, де наша логіка виграє (таймлайн), а де ні |
| Перехід на Gemini і чесна фіксація цього в REPORT | Дочекатись кредитів Claude | Модель — це ручка в налаштуваннях; архітектура від неї не залежить, і краще показати виміряний результат, ніж необмірений |

## Цифри

| Метрика | Значення |
|---|---|
| Завантажено / канонічних документів | 459 / **441** |
| Чанків | 2 138 |
| Дублікатів знайдено | 18 аліасів у 10 кластерах (URL 9 · hash 6 · MinHash 7) |
| AI-інструкцій вирізано | 29 речень у 9 документах |
| Вартість індексу | **$2.19** ($0.044 ембеддинги + $2.141 витяг фактів) |
| Вартість запиту | $0.041 (Opus 5) · $0.011 (Gemini 3.8 Flash) |
| Середня латентність | 4 777 мс |
| Eval: strict / lenient | **70% / 95%** |
| Вигаданих фактів | **0** |
| Негативні кейси (відмова) | 5/5 |
| Помилково відмовився | 1 (q13, продукти з 2025) |

## Що не вийшло / свідомо відкинуто

- **Кредити Claude скінчились посеред оцінювання.** Підсумковий прогін зроблено на Gemini 3.8 Flash; перші 9 питань на Opus 5 (усі correct) збережено окремо. Це записано в REPORT, а не заховано.
- **`security.everstake.com` віддає 403** (Cloudflare) — 8 сторінок втрачено. Обхід не робився свідомо; сертифікації все одно покриті `ai-info`, `about` і блогом.
- **Немає інкрементального re-crawl і планувальника** — `npm run crawl` ідемпотентний, але зміни не детектуються. Це прямо підриває правило «жива сторінка станом на дату фетчу»: воно протухає разом із фетчем.
- **Дедуп на рівні події не реалізовано** — bitget (машинний переклад, 3% перетину) і signalplus (AI-переписування, 0%) описують ту саму подію, але текстова схожість їх не бачить. Задокументовано, не пофіксено.
- **Q13 (продукти з 2025) — помилкова відмова.** У корпусі сторінки є; ретрівер їх не підняв. Це справжня помилка, не пастка.
- **PROCESS.md (Частина B) не написано** — пишеться людиною, не агентом.

## Що далі

1. Adversarial-набір: 20 кейсів (інʼєкції в питанні, підставні документи, хибні передумови) з машинними критеріями pass/fail і canary-токенами.
2. Агентний шар: інструменти, live fetch, стрім пайплайну в UI (`docs/agentic-spec.md`).
3. Інкрементальний re-crawl із change detection — без нього правило живої сторінки деградує.
4. Розбір q13: чому ретрівер не підняв продуктові сторінки.
5. Матеріал для Частини B: внутрішня система для операційного директора.
