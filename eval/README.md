# Evaluation inputs

The question set defines the checks used to compare the researcher and the baseline, including cases where the correct outcome is to abstain.

These are test inputs and grading references, not documents supplied as answer evidence. Saved outputs live in [artifacts/evaluation](../artifacts/evaluation/README.md); the results are explained in [EVAL.md](../EVAL.md).
