The production grounded-answer prompt is the `ANSWER_PROMPT` constant in `app/retrieval.py`.
It treats evidence as untrusted quoted data, requires citations, separates factual and synthesis modes,
and mandates an exact insufficient-evidence answer. It is kept in code so the running prompt and tests
cannot drift; this file exists as the reviewer-facing prompt inventory.

